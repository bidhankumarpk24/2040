(() => {
  'use strict';
  const SIZE = 4, WIN = 2048;
  const storage = { get(k, fallback) { try { const v = localStorage.getItem(k); return v === null ? fallback : v; } catch (_) { return fallback; } }, set(k, v) { try { localStorage.setItem(k, v); } catch (_) {} } };
  let board = [], score = 0, best = Number(storage.get('2048-best', 0)) || 0, previous = null, won = false, ended = false, sound = storage.get('2048-sound', 'on') !== 'off';
  const $ = id => document.getElementById(id), boardEl = $('board'), scoreEl = $('score'), bestEl = $('best'), statusEl = $('status-line'), modal = $('modal');
  const randomTile = () => Math.random() < .9 ? 2 : 4;
  function emptyCells() { const cells = []; board.forEach((row, r) => row.forEach((v, c) => { if (!v) cells.push([r, c]); })); return cells; }
  function addTile() { const cells = emptyCells(); if (!cells.length) return; const [r, c] = cells[Math.floor(Math.random() * cells.length)]; board[r][c] = randomTile(); }
  function newGame() { board = Array.from({ length: SIZE }, () => Array(SIZE).fill(0)); score = 0; previous = null; won = false; ended = false; addTile(); addTile(); closeModal(); update(); }
  function cloneState() { return { board: board.map(row => row.slice()), score, won }; }
  function restore(state) { board = state.board.map(row => row.slice()); score = state.score; won = state.won; ended = false; closeModal(); update(); }
  function slide(line) { const values = line.filter(Boolean), result = [], merges = []; for (let i = 0; i < values.length; i++) { if (values[i] === values[i + 1]) { const value = values[i] * 2; result.push(value); merges.push(result.length - 1); score += value; i++; } else result.push(values[i]); } while (result.length < SIZE) result.push(0); return { result, merges }; }
  function move(direction) { if (ended) return false; const before = board.map(row => row.slice()); let changed = false, merged = false;
    if (direction === 'left' || direction === 'right') { for (let r = 0; r < SIZE; r++) { let line = board[r].slice(); if (direction === 'right') line.reverse(); const out = slide(line); if (direction === 'right') out.result.reverse(); if (out.result.some((v, i) => v !== board[r][i])) changed = true; if (out.merges.length) merged = true; board[r] = out.result; } }
    else { for (let c = 0; c < SIZE; c++) { let line = board.map(row => row[c]); if (direction === 'down') line.reverse(); const out = slide(line); if (direction === 'down') out.result.reverse(); out.result.forEach((v, i) => { if (v !== board[i][c]) changed = true; board[i][c] = v; }); if (out.merges.length) merged = true; } }
    if (!changed) return false; previous = { board: before, score, won }; // score is corrected below using the pre-move snapshot
    previous.score = score - (merged ? calculateMoveGain(before, board) : 0);
    addTile(); if (score > best) { best = score; storage.set('2048-best', best); }
    render(merged); if (!won && board.some(row => row.includes(WIN))) { won = true; showWin(); } else if (!canMove()) { ended = true; showGameOver(); } return true;
  }
  // Calculates only the points generated by merges, keeping Undo exact without a second game state.
  function calculateMoveGain(before, after) { const a = before.flat(), b = after.flat(); let gain = 0; const beforeCounts = {}, afterCounts = {}; a.forEach(v => { if (v) beforeCounts[v] = (beforeCounts[v] || 0) + 1; }); b.forEach(v => { if (v) afterCounts[v] = (afterCounts[v] || 0) + 1; }); Object.keys(afterCounts).forEach(v => { const n = Number(v), merges = Math.max(0, (beforeCounts[n / 2] || 0) - (afterCounts[n / 2] || 0)); gain += merges * n; }); return gain; }
  function canMove() { if (emptyCells().length) return true; for (let r = 0; r < SIZE; r++) for (let c = 0; c < SIZE; c++) if ((r < 3 && board[r][c] === board[r + 1][c]) || (c < 3 && board[r][c] === board[r][c + 1])) return true; return false; }
  function render(merged = false) { boardEl.innerHTML = ''; board.forEach((row, r) => row.forEach((value, c) => { const cell = document.createElement('div'); cell.className = 'cell'; cell.setAttribute('role', 'gridcell'); if (value) { cell.className += ` tile ${value <= WIN ? `v${value}` : 'super'}${merged ? ' merge' : ''}`; cell.textContent = value; cell.setAttribute('aria-label', `Tile ${value}`); } else cell.setAttribute('aria-label', 'Empty'); boardEl.appendChild(cell); })); }
  function update() { scoreEl.textContent = score; bestEl.textContent = best; $('undo').disabled = !previous; render(); }
  function render(merged) { boardEl.innerHTML = ''; board.forEach((row, r) => row.forEach((value, c) => { const cell = document.createElement('div'); cell.className = 'cell'; cell.setAttribute('role', 'gridcell'); if (value) { cell.className += ` tile ${value <= WIN ? `v${value}` : 'super'}${merged ? ' merge' : ''}`; cell.textContent = value; cell.setAttribute('aria-label', `Tile ${value}`); } else cell.setAttribute('aria-label', 'Empty'); boardEl.appendChild(cell); })); }
  function showWin() { $('modal-icon').textContent = '✦'; $('modal-title').textContent = 'You reached 2048!'; $('modal-message').textContent = "Congratulations! You've mastered the board."; $('modal-score').textContent = score; $('modal-primary').textContent = 'Continue Game'; $('modal-secondary').textContent = 'New Game'; $('modal-primary').onclick = closeModal; $('modal-secondary').onclick = newGame; modal.hidden = false; beep(660); }
  function showGameOver() { $('modal-icon').textContent = '◇'; $('modal-title').textContent = 'Game Over'; $('modal-message').textContent = 'No more moves. Try again!'; $('modal-score').textContent = score; $('modal-primary').textContent = 'Try Again'; $('modal-secondary').textContent = 'Close'; $('modal-primary').onclick = newGame; $('modal-secondary').onclick = closeModal; modal.hidden = false; }
  function closeModal() { modal.hidden = true; }
  function beep(freq = 440) { if (!sound) return; try { const ctx = new (window.AudioContext || window.webkitAudioContext)(), osc = ctx.createOscillator(), gain = ctx.createGain(); osc.frequency.value = freq; gain.gain.setValueAtTime(.035, ctx.currentTime); gain.gain.exponentialRampToValueAtTime(.001, ctx.currentTime + .12); osc.connect(gain); gain.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + .12); } catch (_) {} }
  function share() { const text = `I scored ${score} points in 2048 Master! Can you beat my score?`; if (navigator.share) navigator.share({ title: '2048 Master', text }).catch(() => {}); else if (navigator.clipboard) navigator.clipboard.writeText(text).then(() => { statusEl.textContent = 'Score copied to your clipboard!'; }).catch(() => {}); }
  document.addEventListener('keydown', e => { const keys = { ArrowUp: 'up', ArrowDown: 'down', ArrowLeft: 'left', ArrowRight: 'right', w: 'up', a: 'left', s: 'down', d: 'right' }; const dir = keys[e.key]; if (dir) { e.preventDefault(); if (move(dir)) beep(330); } });
  let touchStart = null; boardEl.addEventListener('touchstart', e => { const t = e.changedTouches[0]; touchStart = { x: t.clientX, y: t.clientY }; }, { passive: true }); boardEl.addEventListener('touchend', e => { if (!touchStart) return; const t = e.changedTouches[0], dx = t.clientX - touchStart.x, dy = t.clientY - touchStart.y; touchStart = null; if (Math.max(Math.abs(dx), Math.abs(dy)) < 25) return; const dir = Math.abs(dx) > Math.abs(dy) ? (dx > 0 ? 'right' : 'left') : (dy > 0 ? 'down' : 'up'); if (move(dir)) beep(330); }, { passive: true });
  $('new-game').onclick = newGame; $('undo').onclick = () => { if (previous) restore(previous); }; $('share').onclick = share; $('theme-toggle').onclick = () => { const light = document.documentElement.dataset.theme === 'light'; document.documentElement.dataset.theme = light ? 'dark' : 'light'; storage.set('2048-theme', light ? 'dark' : 'light'); $('theme-toggle').textContent = light ? '☼' : '☾'; $('theme-toggle').setAttribute('aria-label', light ? 'Switch to light mode' : 'Switch to dark mode'); }; $('sound-toggle').onclick = () => { sound = !sound; storage.set('2048-sound', sound ? 'on' : 'off'); $('sound-toggle').textContent = sound ? '🔊' : '🔇'; $('sound-toggle').setAttribute('aria-label', sound ? 'Turn sound off' : 'Turn sound on'); if (sound) beep(); }; modal.addEventListener('click', e => { if (e.target === modal) closeModal(); });
  const savedTheme = storage.get('2048-theme', 'dark'); document.documentElement.dataset.theme = savedTheme; $('theme-toggle').textContent = savedTheme === 'light' ? '☾' : '☼'; $('sound-toggle').textContent = sound ? '🔊' : '🔇'; bestEl.textContent = best; newGame();
})();
